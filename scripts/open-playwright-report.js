// import { execFile } from 'node:child_process';
// import path from 'node:path';

// const reportPath = path.resolve('playwright-report', 'index.html');

// if (process.platform === 'win32') {
//   execFile('cmd', ['/c', 'start', '', reportPath], { windowsHide: true });
// } else if (process.platform === 'darwin') {
//   execFile('open', [reportPath]);
// } else {
//   execFile('xdg-open', [reportPath]);
// }

// console.log(`Opened Playwright report: ${reportPath}`);
